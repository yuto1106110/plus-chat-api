datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ユーザー情報のテーブル
model User {
  id       Int    @id @default(autoincrement())
  username String @unique
  password String
}
